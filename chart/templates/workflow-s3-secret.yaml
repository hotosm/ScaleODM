{{- /*
workflow-s3-secret.yaml intentionally renders nothing.

Workflow pods now consume credentials from the unified runtime secret specified by
AWS_S3_SECRET_NAME (set by deployment.yaml), so no separate workflow S3 secret
resource is created by the chart.
*/ -}}
