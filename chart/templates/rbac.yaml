{{- if .Values.kubernetes.rbac.create }}
# Role in the release namespace so the ScaleODM API can manage workflows.
# Argo controller watches all namespaces, so workflows created here are
# picked up automatically.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "scaleodm.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "scaleodm.labels" . | nindent 4 }}
rules:
  # Permissions needed for Argo Workflows
  - apiGroups:
      - argoproj.io
    resources:
      - workflows
      - workflowtemplates
      - cronworkflows
    verbs:
      - create
      - get
      - list
      - watch
      - update
      - patch
      - delete
  # Permissions for pods (to check workflow status)
  - apiGroups:
      - ""
    resources:
      - pods
      - pods/log
    verbs:
      - get
      - list
      - watch
  # Permissions for events (for workflow status)
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - get
      - list
      - watch
---
# RoleBinding in the release namespace.
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "scaleodm.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "scaleodm.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "scaleodm.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "scaleodm.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
