name: 🧪 Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  tests:
    uses: hotosm/gh-workflows/.github/workflows/just.yml@4.0.1
    with:
      environment: "test"
      command: "test all"
    secrets: inherit
