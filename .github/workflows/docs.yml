name: 📚 Publish Docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    uses: hotosm/gh-workflows/.github/workflows/just.yml@4.0.1
    with:
      environment: "test"
      command: "docs publish"
    secrets: inherit
