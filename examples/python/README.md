# Python Test

- Ensure you have a `.env` file in the repo root.
- See `example-python` in the Justfile for the example command.
- Set `SCALEODM_WORKFLOW_S3_ENDPOINT` in `.env` to an endpoint reachable from workflow pods.
  Default is `http://host.docker.internal:31102` for local Docker/Talos setups.
